# Ingress resource for the application
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devops-implementation
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: devops-implementation.local
    http:
      paths: 
      - path: /
        pathType: Prefix
        backend:
          service:
            name: devops-implementation
            port:
              number: 80

